@model IGrid<BotSpiel.DataAccess.Models.PickBatches>

@{
    ViewBag.Title = "Index Pick Batches Export";
}
<br>
<h2>Pick Batches Export</h2>

<p>
    <a href="@(Url.Action("ExportIndex") + ViewContext.HttpContext.Request.QueryString)">Export</a>
    @Html.ActionLink(" | Grid", "Index")
</p>

@(new HtmlGrid<BotSpiel.DataAccess.Models.PickBatches>(Html, Model))
 

