

@model IEnumerable<BotSpiel.DataAccess.Models.PickBatches>

@{
    ViewBag.Title = "Index Pick Batches";
}
<br>
<h2>Pick Batches</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('PickBatches')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "pickbatches-ajax-grid" })
 

