

@model IEnumerable<BotSpiel.DataAccess.Models.BusinessPartners>

@{
    ViewBag.Title = "Index Business Partners";
}
<br>
<h2>Business Partners</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('BusinessPartners')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "businesspartners-ajax-grid" })
 

