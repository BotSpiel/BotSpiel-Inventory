

@model IEnumerable<BotSpiel.DataAccess.Models.OutboundCarrierManifests>

@{
    ViewBag.Title = "Index Outbound Carrier Manifests";
}
<br>
<h2>Outbound Carrier Manifests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('OutboundCarrierManifests')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "outboundcarriermanifests-ajax-grid" })
 

