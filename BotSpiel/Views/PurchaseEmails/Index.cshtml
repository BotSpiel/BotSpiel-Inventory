

@model IEnumerable<BotSpiel.DataAccess.Models.PurchaseEmails>

@{
    ViewBag.Title = "Index Purchase Emails";
}
<br>
<h2>Purchase Emails</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('PurchaseEmails')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "purchaseemails-ajax-grid" })
 

