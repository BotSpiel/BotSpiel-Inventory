

@model IEnumerable<BotSpiel.DataAccess.Models.PaymentAddresses>

@{
    ViewBag.Title = "Index Payment Addresses";
}
<br>
<h2>Payment Addresses</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('PaymentAddresses')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "paymentaddresses-ajax-grid" })
 

