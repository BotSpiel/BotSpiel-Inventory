

@model IEnumerable<BotSpiel.DataAccess.Models.InventoryUnits>

@{
    ViewBag.Title = "Index Inventory Units";
}
<br>
<h2>Inventory Units</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('InventoryUnits')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "inventoryunits-ajax-grid" })
 

