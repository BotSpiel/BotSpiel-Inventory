

@model IEnumerable<BotSpiel.DataAccess.Models.RequestsForInformation>

@{
    ViewBag.Title = "Index Requests For Information";
}
<br>
<h2>Requests For Information</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
    <button type="button" class="btn btn-default delete-rows" onclick="deleteRows('RequestsForInformation')">Delete Selected Rows</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "requestsforinformation-ajax-grid" })
 

