

@model IEnumerable<BotSpiel.DataAccess.Models.CountryLocations>

@{
    ViewBag.Title = "Index Country Locations";
}
<br>
<h2>Country Locations</h2>

<p>
    @Html.ActionLink(" | Grid Export", "IndexExport")
</p>

<p>    
    <button type="button" id="refresh-grid" class="btn btn-default content-refresh">Refresh Grid</button>
</p>

@Html.AjaxGrid(Url.Action("IndexGrid"), new { id = "countrylocations-ajax-grid" })
 

